# .github/workflows/stylelint.yml
name: Lint Stylus with Stylelint

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Stylelint deps
        run: |
          npm init -y
          npm install --save-dev stylelint@14 stylelint-processor-stylus postcss-styl@0.0.3
     
      - name: Run Stylelint
        run: |
          npx stylelint "**/*.{styl,uso,css}" --config stylelint.config.json
